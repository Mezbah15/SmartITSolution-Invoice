@using SmartITSolution_Invoice.Models
@model InvoiceViewModel
@{
    ViewData["Title"] = "SmartITSolution";
}
<!-- Form Section -->
<section class="container-fluid">
    <div class="row g-4">

        <!-- Left Side (3 Portion) -->
        <div class="col-lg-7">
            <div class="card shadow border-0">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0 text-center">SMART IT INVOICE SYSTEM</h3>
                </div>

                <div class="card-body">
                    <form asp-action="Invoice"
                          asp-controller="Home"
                          method="post"
                          id="invoiceForm">

                        <div asp-validation-summary="ModelOnly"
                             class="alert alert-danger"></div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="InvoiceNo" class="form-label fw-bold">
                                    Invoice Number
                                </label>
                                <input asp-for="InvoiceNo" class="form-control" readonly />
                                <span asp-validation-for="InvoiceNo" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label asp-for="InvoiceDate" class="form-label fw-bold">
                                    Date
                                </label>
                                <input asp-for="InvoiceDate" type="date" class="form-control" />
                                <span asp-validation-for="InvoiceDate" class="text-danger"></span>
                            </div>
                        </div>

                        <!-- Client Details -->
                        <div class="mb-3">
                            <label asp-for="CustomerDetails" class="form-label fw-bold">
                                Client Details
                            </label>

                            <textarea asp-for="CustomerDetails"
                                      rows="5"
                                      class="form-control"
                                      placeholder="Company Name&#10;Address&#10;Phone&#10;Email"></textarea>

                            <span asp-validation-for="CustomerDetails"
                                  class="text-danger"></span>
                        </div>

                        <!-- Invoice Items -->
                        <div class="mb-3">
                            <table class="table table-bordered table-striped" id="itemsTable">
                                <thead class="table-primary">
                                    <tr>
                                        <th>Description</th>
                                        <th width="120">Quantity</th>
                                        <th width="120">Rate</th>
                                        <th width="150">Amount</th>
                                        <th width="80">Remove</th>
                                    </tr>
                                </thead>

                                <tbody id="itemBody">
                                    <tr>
                                        <td>
                                            <input name="Items[0].Description"
                                                   class="form-control" />
                                        </td>

                                        <td>
                                            <input name="Items[0].Quantity"
                                                   type="number"
                                                   class="form-control qty" />
                                        </td>

                                        <td>
                                            <input name="Items[0].Rate"
                                                   type="number"
                                                   class="form-control rate" />
                                        </td>

                                        <td>
                                            <input class="form-control amount" readonly />
                                        </td>

                                        <td class="text-center">
                                            <button type="button"
                                                    class="btn btn-danger remove-row">
                                                X
                                            </button>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>

                            <button type="button"
                                    class="btn btn-primary"
                                    id="addRow">
                                + Add Item
                            </button>
                        </div>

                        <!-- Totals -->
                        <div class="row mt-3">
                            <div class="col-md-4 offset-md-8">

                                <!-- Grand Total -->
                                <div class="input-group mb-2">
                                    <span class="input-group-text fw-bold">
                                        Grand Total
                                    </span>

                                    <input id="grandTotal"
                                           class="form-control fw-bold text-end"
                                           readonly />
                                </div>
                                <!-- This Discount field added as a new feature 31.08.26-->
                                <!-- Discount--> 
                                <div class="input-group mb-2">
                                    <span class="input-group-text fw-bold">
                                        Discount
                                    </span>

                                    <input asp-for="Discount"
                                           id="discount"
                                           type="number"
                                           class="form-control text-end bg-warning-subtle text-warning-emphasis fw-bold"
                                           value="0" />
                                </div>
                                <!-- Advance Payment -->
                                <div class="input-group mb-2">
                                    <span class="input-group-text fw-bold">
                                        Advance
                                    </span>

                                    <input asp-for="AdvancePayment"
                                           id="advancePayment"
                                           type="number"
                                           class="form-control text-end bg-success-subtle text-success fw-bold"
                                           value="0" />
                                </div>

                                <!-- Due Amount -->
                                <div class="input-group">
                                    <span class="input-group-text fw-bold">
                                        Due Amount
                                    </span>

                                    <input id="dueAmount"
                                           class="form-control fw-bold text-end"
                                           readonly />
                                </div>

                            </div>
                        </div>

                        <!-- Note -->
                        <div class="mb-3">
                            <label asp-for="Note" class="form-label fw-bold">
                                Note If Any
                            </label>

                            <textarea asp-for="Note"
                                      rows="3"
                                      class="form-control"></textarea>

                            <span asp-validation-for="Note"
                                  class="text-danger"></span>
                        </div>

                        <button type="submit"
                                class="btn btn-outline-primary btn-lg w-100"
                                id="cmd">
                            Generate Invoice
                        </button>

                    </form>
                </div>
            </div>
        </div>

        <!-- Right Side (1 Portion) -->
        <div class="col-lg-5">
            <!--PAYMENT ACKNOWLEDGEMENT: Instant Payment PDF-->
            <div class="card shadow border-0">
                <div class="card-header bg-success text-white">
                    <h4 class="mb-0 text-center">PAYMENT ACKNOWLEDGEMENT</h4>
                </div>

                <div class="card-body">
                    <form asp-action="PaymentAcknowledgement"
                          asp-controller="Home"
                          method="post"
                          id="invoiceForm">

                        <div asp-validation-summary="ModelOnly"
                             class="alert alert-danger"></div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="InvoiceNo" class="form-label fw-bold">
                                    Invoice Number
                                </label>
                                <input asp-for="InvoiceNo" class="form-control" readonly />
                                <span asp-validation-for="InvoiceNo" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label asp-for="InvoiceDate" class="form-label fw-bold">
                                    Date
                                </label>
                                <input asp-for="InvoiceDate" type="date" class="form-control" />
                                <span asp-validation-for="InvoiceDate" class="text-danger"></span>
                            </div>
                        </div>

                        <!-- Client Details -->
                        <div class="mb-3">
                            <label asp-for="CustomerDetails" class="form-label fw-bold">
                                Client Details
                            </label>

                            <textarea asp-for="CustomerDetails"
                                      rows="5"
                                      class="form-control"
                                      placeholder="Company Name&#10;Address&#10;Phone&#10;Email"></textarea>

                            <span asp-validation-for="CustomerDetails"
                                  class="text-danger"></span>
                        </div>

                        <!-- Amount In Payment -->
                        <div class="mb-3">
                            <label asp-for="CustomerDetails" class="form-label fw-bold">
                                Amount In Payment
                            </label>
                            <textarea asp-for="@Model.PaymentACKViewModel.Amount"
                                  rows="1"
                                  class="form-control"
                                  placeholder="10000"></textarea>

                            <span asp-validation-for="@Model.PaymentACKViewModel.Amount"
                              class="text-danger"></span>
                        </div>
                        <!-- Note -->
                        <div class="mb-3">
                            <label asp-for="Note" class="form-label fw-bold">
                                Note If Any
                            </label>

                            <textarea asp-for="Note"
                                      rows="3"
                                      class="form-control"></textarea>

                            <span asp-validation-for="Note"
                                  class="text-danger"></span>
                        </div>

                        <button type="submit"
                                class="btn btn-outline-success btn-lg w-100"
                                id="cmd">
                            Generate Acknowledgement
                        </button>

                    </form>
                </div>
            </div>
            <!--PAID STAMP: Upload Existing PDF-->
            <div class="card shadow border-0" style="margin-top: 40px">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Upload Existing PDF</h5>
                </div>

                <div class="card-body">
                    <form id="uploadForm" asp-action="UploadPdf"
                          asp-controller="Home"
                          method="post"
                          enctype="multipart/form-data">

                        <div class="mb-3">
                            <label class="form-label">Select PDF</label>

                            <input id="pdfFile"
                                   class="form-control"
                                   type="file"
                                   name="pdfFile"
                                   accept=".pdf" />
                        </div>

                        <button class="btn btn-outline-danger w-100">
                            Upload &amp; Add PAID
                        </button>
                    </form>
                    <div>@TempData["Success"]</div>
                </div>
            </div>
            </div>
        </div>
</section>


@section Scripts {
    <script src="~/js/invoice.js"></script>
    <script src="~/js/PDFeditorAJAX.js"></script>
}